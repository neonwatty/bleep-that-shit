<div class="bg-gradient-to-br from-gray-100 via-indigo-50 to-pink-50 min-h-screen pb-16 rounded-2xl border border-gray-200 shadow-xl">
  <div class="max-w-3xl mx-auto bg-white/80 rounded-2xl shadow-2xl p-8 mt-8 mb-16 border border-gray-200 backdrop-blur">
    <!-- Purpose Banner -->
    <div class="mb-6 p-4 bg-blue-100 border-l-4 border-blue-500 rounded-2xl shadow">
      <div class="flex items-center">
        <span class="text-2xl mr-3">🎛️</span>
        <div>
          <h1 class="text-2xl font-bold mb-1">Transcription Sampler</h1>
          <p class="text-blue-800">
            Quickly test different transcription models on a short snippet (first <strong>5 seconds</strong>) of your audio or video.<br>
            Compare speed and accuracy before processing the full file.
          </p>
        </div>
      </div>
    </div>

    <!-- How it works -->
    <div class="mb-2 text-sm font-semibold text-gray-700 uppercase tracking-wide">How it works</div>
    <ol class="mb-6 pl-6 list-decimal text-gray-700">
      <li><span class="mr-1">🎵</span><strong>Upload a file</strong> (audio or video, max 5 seconds will be used for preview).</li>
      <li><span class="mr-1">🗣️</span><strong>Select a model</strong> to see how it performs on the snippet.</li>
      <li><span class="mr-1">🔍</span><strong>Transcribe and review the transcript and timing.</strong></li>
      <li><span class="mr-1">🚀</span>When satisfied, use the <a href="/bleep-view" class="text-blue-600 underline hover:text-blue-800 font-semibold">main tool</a> to process the full file and bleep words.</li>
    </ol>

    <!-- Step 1: Upload File -->
    <section class="step mb-8 p-6 border-l-4 border-blue-400 bg-blue-50 rounded-2xl shadow-xl border border-blue-100">
      <div class="flex items-center mb-2">
        <span class="step-number bg-blue-400 text-white rounded-full w-8 h-8 flex items-center justify-center mr-3 font-bold">1</span>
        <h2 class="text-lg font-semibold">Upload Your File</h2>
      </div>
      <p class="mb-2 text-sm text-blue-900">Audio (MP3) or Video (MP4) supported. Only the first 5 seconds will be used for preview.</p>
      <div id="dropzone" class="border-2 border-dashed border-gray-300 rounded-lg p-8 text-center cursor-pointer hover:border-blue-500 transition-colors">
        <p class="text-gray-600">Drag and drop your audio or video file here or click to browse</p>
        <input type="file" id="fileInput" accept="audio/*,video/mp4" class="hidden">
      </div>
      <div id="transcriptionFileWarning" class="mt-2"></div>
      <details id="inputPreviewSection" class="mt-6 hidden">
        <summary class="cursor-pointer text-lg font-semibold text-gray-700 select-none">Preview Input File (Audio/Video)</summary>
        <div id="inputPreviewContainer" class="mt-4">
          <!-- Player will be injected here by JS -->
        </div>
      </details>
    </section>

    <!-- Step 2: Select Language & Model -->
    <section class="step mb-8 p-6 border-l-4 border-green-400 bg-green-50 rounded-2xl shadow-xl border border-green-100">
      <div class="flex items-center mb-2">
        <span class="step-number bg-green-400 text-white rounded-full w-8 h-8 flex items-center justify-center mr-3 font-bold">2</span>
        <h2 class="text-lg font-semibold">Select Language & Model</h2>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <label for="language" class="block text-sm font-medium text-gray-700 mb-2">Language</label>
          <select id="language" class="w-full p-2 border border-gray-300 rounded-md">
            <option value="en">English</option>
            <option value="es">Spanish</option>
            <option value="fr">French</option>
            <option value="de">German</option>
            <option value="it">Italian</option>
            <option value="pt">Portuguese</option>
            <option value="nl">Dutch</option>
            <option value="pl">Polish</option>
            <option value="ru">Russian</option>
            <option value="ja">Japanese</option>
            <option value="ko">Korean</option>
            <option value="zh">Chinese</option>
          </select>
        </div>
        <div>
          <label for="model" class="block text-sm font-medium text-gray-700 mb-2">Model</label>
          <select id="model" class="w-full p-2 border border-gray-300 rounded-md">
            <option value="onnx-community/whisper-tiny_timestamped">Tiny (Smallest, fastest, least accurate)</option>
            <option value="onnx-community/whisper-base_timestamped">Base (Faster, less accurate)</option>
            <option value="onnx-community/whisper-small_timestamped" selected>Small (Balanced)</option>
            <option value="onnx-community/whisper-large-v3-turbo_timestamped">Large (Slowest, most accurate)</option>
          </select>
          <div class="text-xs text-gray-500 mt-1">
            <span class="mr-2">⏱️ = speed</span>
            <span class="mr-2">🎯 = accuracy</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Step 3: Transcribe -->
    <section class="step mb-8 p-6 border-l-4 border-indigo-400 bg-indigo-50 rounded-2xl shadow-xl border border-indigo-100">
      <div class="flex items-center mb-2">
        <span class="step-number bg-indigo-400 text-white rounded-full w-8 h-8 flex items-center justify-center mr-3 font-bold">3</span>
        <h2 class="text-lg font-semibold">Transcribe</h2>
      </div>
      <button id="transcribeButton" class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700">Transcribe</button>
      <div class="mt-6">
        <div class="w-full bg-gray-200 rounded-full h-2.5">
          <div id="progress" class="bg-blue-600 h-2.5 rounded-full transition-all duration-300" style="width: 0%"></div>
        </div>
        <div id="progressText" class="text-center text-sm text-gray-600 mt-2">0%</div>
      </div>
      <div id="error" class="mt-4 p-4 bg-red-100 text-red-700 rounded-md hidden"></div>
    </section>

    <!-- Step 4: Review Results -->
    <section class="step mb-8 p-6 border-l-4 border-purple-400 bg-purple-50 rounded-2xl shadow-xl border border-purple-100">
      <div class="flex items-center mb-2">
        <span class="step-number bg-purple-400 text-white rounded-full w-8 h-8 flex items-center justify-center mr-3 font-bold">4</span>
        <h2 class="text-lg font-semibold">Review Transcription Results</h2>
      </div>
      <details id="results" class="mt-2 hidden">
        <summary class="cursor-pointer text-lg font-semibold text-gray-700 select-none">View Transcript & Word Timestamps</summary>
        <div class="mt-4">
          <!-- Results will be inserted here by JavaScript -->
        </div>
      </details>
    </section>

    <!-- Call to Action for Full Workflow -->
    <div class="mt-8 text-center">
      <div class="inline-block bg-green-50 border border-green-400 text-green-700 px-6 py-3 rounded shadow">
        <span class="mr-2">✅</span>
        <span>
          Ready to process the full file? 
          <a href="/bleep-view" class="text-green-700 underline font-semibold hover:text-green-900">Go to Main Tool</a>
        </span>
      </div>
    </div>
  </div>
</div> 