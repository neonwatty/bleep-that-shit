<nav class="flex items-center justify-between py-4 mb-8 border-b">
  <div class="text-xl font-bold">
    Bleep That Sh*t!
  </div>
  <div class="space-x-4 flex items-center">
    <% if logged_in? %>
      <span class="text-gray-700 mr-2">Signed in as <%= current_user.email %></span>
      <%= button_to "Sign Out", session_path(current_user), method: :delete, class: "bg-red-600 text-white px-4 py-2 rounded" %>
    <% end %>
  </div>
  <div class="flex items-center space-x-4">
    <a href="/transcription-sampler-view" class="flashing-btn bg-gradient-to-r from-green-500 via-green-400 to-green-600 text-white px-4 py-2 rounded font-semibold shadow-lg transform transition-all duration-200 hover:scale-105 hover:from-green-400 hover:to-green-500 hover:animate-gradient-x">Transcription Sampler</a>
    <a href="/bleep-view" class="flashing-btn bg-gradient-to-r from-violet-500 via-pink-500 to-violet-700 text-white px-4 py-2 rounded font-semibold ml-4 shadow-lg transform transition-all duration-200 hover:scale-105 hover:from-pink-500 hover:to-violet-500 hover:animate-gradient-x">Bleep Your Sh*t!</a>
  </div>
</nav>

<style>
@keyframes gradient-x {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}
.flashed-btn, .flashing-btn {
  background-size: 200% 200%;
}
.hover\:animate-gradient-x:hover {
  animation: gradient-x 2s ease-in-out infinite;
}
</style> 