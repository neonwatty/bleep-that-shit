<nav class="flex items-center justify-between py-4 mb-8 border-b">
  <div class="text-xl font-bold">
    <%= link_to "Bleep That Sh*t!", root_path, class: "hover:text-blue-600" %>
  </div>
  <div class="space-x-4 flex items-center">
    <% if logged_in? %>
      <span class="text-gray-700 mr-2">Signed in as <%= current_user.email %></span>
      <%= button_to "Sign Out", session_path(current_user), method: :delete, class: "bg-red-600 text-white px-4 py-2 rounded" %>
    <% end %>
  </div>
  <div class="flex items-center space-x-4">
    <a href="/transcription-sampler-view" class="text-blue-700 hover:underline font-semibold">Transcription Sampler</a>
    <a href="/bleep-view" class="text-indigo-700 hover:underline font-semibold">Bleep View</a>
  </div>
</nav> 