<nav class="w-full max-w-4xl mx-auto flex flex-col sm:flex-row items-center justify-between py-6 px-2 md:px-0 mb-4 border-b-2 border-black">
  <div class="text-3xl md:text-4xl font-extrabold uppercase tracking-tight text-black mb-4 sm:mb-0">
    <a href="/" class="hover:underline">Bleep That Sh*t!</a>
  </div>




  <div class="flex flex-col sm:flex-row items-center gap-3 sm:gap-6 w-full sm:w-auto">
    <a href="/bleep-view" class="bg-black text-white font-bold px-6 py-2 rounded-full uppercase tracking-wider text-base hover:bg-gray-900 transition-all w-full sm:w-auto text-center">Bleep Your Sh*t!</a>
    <a href="/transcription-sampler-view" class="bg-pink-500 text-white font-bold px-6 py-2 rounded-full uppercase tracking-wider text-base hover:bg-pink-600 transition-all w-full sm:w-auto text-center">Transcription Sampler</a>
    <% if logged_in? %>
      <span class="text-gray-700 ml-2 text-xs sm:text-sm">Signed in as <%= current_user.email %></span>
      <%= button_to "Sign Out", session_path(current_user), method: :delete, class: "bg-pink-500 text-white font-bold px-6 py-2 rounded-full uppercase tracking-wider text-base hover:bg-pink-600 transition-all ml-2 w-full sm:w-auto text-center" %>
    <% end %>
  </div>
</nav>

<style>
@keyframes gradient-x {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}
.flashed-btn, .flashing-btn {
  background-size: 200% 200%;
}
.hover\:animate-gradient-x:hover {
  animation: gradient-x 2s ease-in-out infinite;
}
</style> 