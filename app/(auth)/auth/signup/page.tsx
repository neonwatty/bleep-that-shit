import { SignupForm } from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Create Account',
  description: 'Create a Bleep That Sh*t! account to access premium features.',
};

export default function SignupPage() {
  return <SignupForm />;
}
